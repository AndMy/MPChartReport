<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hmsg.hospitalreport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:name=".App">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.LoginActivity" />
        <activity android:name=".ui.DialogueReportActivity" />
        <activity android:name=".ui.ReplyReportActivity" />
        <activity android:name=".ui.DoctorRegisteredActivity" />
        <activity android:name=".ui.DoctorDataActivity" />
        <activity android:name=".ui.TimeoutActivity" />
        <activity android:name=".ui.StoreSalesActivity" />
    </application>

</manifest>